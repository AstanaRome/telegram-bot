# Tomorrow School Telegram Bot

Простой бот, который считает время учебных сессий за календарную неделю (понедельник – воскресенье) и показывает статистику.

## Что нужно

- Python 3.10+
- Токен Telegram-бота из [@BotFather](https://t.me/BotFather)
- Установка зависимостей: `pip install -r requirements.txt`
- Переменная окружения `TELEGRAM_BOT_TOKEN` с токеном бота

## Возможности

- Клавиатура с кнопками «Начать сессию», «Закончить сессию», «Статистика недели»
- Учёт длительности каждой сессии (в минутах)
- Сохранение данных в `data/sessions.json`
- Подсчёт времени только в рамках текущей недели (пон-Вс)

## Запуск

```bash
python bot.py
```

Бот хранит данные локально, поэтому достаточно запустить его на постоянном сервере (например, Render, Railway, Fly.io или любой VPS) либо на домашнем ПК, который остаётся включённым. Перед деплоем рекомендуются менеджеры процессов (systemd, PM2, Docker) для авто‑рестартов.

## Дальнейшие улучшения

- Добавить БД (PostgreSQL/SQLite) вместо JSON для многопользовательского сценария
- Добававить отчёты за произвольный период и экспорт в XLSX/CSV
- Интегрировать авторизацию/привязку к ученикам Tomorrow School
